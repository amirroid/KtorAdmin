package ir.amirroid.ktoradmin.templates.fluent

import ir.amirroid.ktoradmin.template.FontFamily

object FluentFontFamily {
    val SegoeUi =
        FontFamily(
            name = "Segoe UI",
            regular = null,
            bold = null,
            stylesheet = null,
            fallbacks = listOf("-apple-system", "BlinkMacSystemFont", "Roboto", "Helvetica", "Arial", "sans-serif"),
        )
}
