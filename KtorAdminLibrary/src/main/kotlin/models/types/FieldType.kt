package models.types

enum class FieldType {
    STRING,
    INTEGER,
    LONG,
    DOUBLE,
    FLOAT,
    BOOLEAN,
    DATE,
    DATETIME,
    BINARY,
    DECIMAL128,
    OBJECT_ID,
    NOT_AVAILABLE
}